# To run this application w/o updating the JAVA_HOME if you # already #have java_home setup for some other service.

cmd> $env:JAVA_HOME = "C:\Program Files\Java\jdk-25.0.2"
.\mvnw.cmd spring-boot:run

